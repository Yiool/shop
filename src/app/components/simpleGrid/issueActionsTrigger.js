/**
 * Created by Administrator on 2017/3/6.
 */
/*(function () {
  'use strict';
  angular
    .module('shopApp')
    .directive('issueActionsTrigger', issueActionsTrigger);


  /!**
   * 更多操作  issueActionsTrigger
   * 1，exportData上挂载api地址 以,隔开
   * 2，params 上双向绑定参数地址，实时获取用户选择的数据
   * 3，监听数据变化，赋值给href然后点击点击按钮导出
   *!/

  /!** @ngInject *!/
  function issueActionsTrigger($document) {
    return {
      restrict: "A",
      scope: {
        actions: "="
      },
      link: function(scope, iElement, iAttrs){
        iElement.on('click', function () {
          show();
        });
        /!**
         * 显示
         *!/
        function show(){

        }

        /!**
         * 创建
         *!/
        function create() {

        }

        /!**
         * 关闭
         *!/
        function hide() {

        }


      }
    }
  }
})();*/
